name: Inspect RC11.2 UI

on:
  push:
    branches:
      - v1.1.0-rc11-release
    paths:
      - .github/workflows/inspect-rc11-2-ui.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download exact RC11.2 package
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          rm -rf build
          mkdir build
          gh release download v1.1.0-rc.11.2 --pattern 'DeckyShare-v1.1.0-rc.11.2.zip' --dir .
          unzip -q DeckyShare-v1.1.0-rc.11.2.zip -d build
          test -f build/DeckyShare/dist/index.js
      - name: Upload UI source
        uses: actions/upload-artifact@v4
        with:
          name: rc11-2-ui-source
          path: |
            build/DeckyShare/dist/index.js
            build/DeckyShare/package.json
            build/DeckyShare/main.py
          retention-days: 1
